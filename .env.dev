# Development Environment Variables for Docker Compose
# Copy this to .env and fill in your actual values

# OpenAI API Key (required for AI features)
OPENAI_API_KEY=your_openai_api_key_here

# JWT Secret (for development - in production use a strong secret)
JWT_SECRET=dev-secret-key-min-32-characters-long-for-testing

# Database URL (automatically set in docker-compose.dev.yml)
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/agenticcms
